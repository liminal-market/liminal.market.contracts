import Network from "./Network";

export default class {{NetworkName}} extends Network {

    constructor() {
        super();
        this.chainId = "{{ChainId}}";
        this.name = "{{NetworkName}}";
        this.liminalMarketAddress = "{{LIMINAL_MARKET_ADDRESS}}";
    }
}